<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/actionBarTextColor</item>
        <item name="android:windowBackground">@color/background_color</item>
        <item name="android:navigationBarColor">@color/colorPrimaryDark</item>
        <!--  Hamburger colour change-->
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>



        <!--OverFlow menu background colour-->
        <item name="android:itemBackground">@color/overflow_menu_background_colour</item>
        <!--Share action provider menu background change code -->
        <item name="android:listPopupWindowStyle">@style/MyListPopupWindowStyle</item>
    </style>

    <style name="MyListPopupWindowStyle" parent="android:Widget.Material.Light.ListPopupWindow">
        <!--Share action provider menu background colour -->
        <item name="android:popupBackground">@color/share_action_background_colour</item>

    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/actionBarTextColor</item>
    </style>


</resources>
